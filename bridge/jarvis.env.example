# Jarvis DingTalk bridge env. Copy to bridge/jarvis.env and fill. NO real secrets in git.
# 由 bridge/run.sh start 自动 source(与 bootstrap/.env 一起); 缺 DINGTALK_APP_KEY/SECRET 时
# run.sh 自动以降级模式点火(JARVIS_NO_DINGTALK=1), 配好凭证后 bridge/run.sh restart 即回全功能。
# 无钉钉降级模式(先点火, 钉钉凭证以后再配): 设为 1 即跳过 DingTalk client/stream/TataPool,
# 只起自动派发 + Scan/Reconcile/Board/Probe/Revisit/Wait 调度器; 卡片/播报降级为 [BROADCAST]
# 日志行(落 bot.log); 挂起/唤醒照常(轮询走 a1), @人通知降级为日志 + 既有 Aone 评论。
# 配好下面 DINGTALK_* 后去掉本行(或设 0)即回全功能模式。
# JARVIS_NO_DINGTALK=1
DINGTALK_APP_KEY=your_app_key
DINGTALK_APP_SECRET=your_app_secret
DINGTALK_TEMPLATE_ID=your_card_template_id
# Robot code: required to create the AI card; defaults to app key if unset.
DINGTALK_ROBOT_CODE=your_robot_code
DINGTALK_STAFF_ID=320687
# Tata 受众名单(逗号分隔): 谁能跟 Tata 门面闲聊。空/不设 = 全员放行。
JARVIS_TATA_STAFF=
# 唯一能让 Tata 升级到重型 Jarvis 的 staffId, 默认辰羿 320687。
JARVIS_MASTER_STAFF=320687
# Tata 的 cwd: 空目录, 不加载 jarvis bootstrap。默认 ~/.jarvis/tata-cwd。
# JARVIS_TATA_ROOT=/Users/you/.jarvis/tata-cwd
# cwd for headless Jarvis claude rounds (default: repo root two levels above bridge/)
# JARVIS_ROOT=/Users/you/cloudspec/jarvis
# Stable queue-pull Worker identity (default: $JARVIS_ROOT/.my-day/bridge/worker-id).
# Keep this file across bridge restarts; each process still gets a fresh processUuid.
# JARVIS_WORKER_ID_FILE=/Users/you/cloudspec/jarvis/.my-day/bridge/worker-id
# claude binary (default: PATH or ~/.local/bin/claude)
# CLAUDE_BIN=/Users/you/.local/bin/claude
CLAUDE_TIMEOUT=300
# Tata=idea: 常驻保温进程基命令 claude --settings <档>。默认 ~/.claude/idea_settings.json。
# Tata 门面(闲聊/委派分诊)属「其他工作」车道；如需与 Jarvis 其他线同走 glm5.2, 指到同一档即可。
# JARVIS_TATA_SETTINGS=/Users/you/.claude/idea_settings.json
#
# --- 模型分层：两条车道，派发点按 _is_terraform_ticket 分流 ---
# 每个「档」是一个 Claude Code settings JSON(env: ANTHROPIC_BASE_URL/MODEL/AUTH_TOKEN)，指向某网关。
# 单档链语法: `:` 分隔=摊额度池(按 session_id 确定性粘档, 同单 resume 稳定);
#             `,` 分隔=failover 主备档链(主档探活失败自动顶到备档)。
# 车道随会话持久化(in-flight / suspend 记录带 terraform 字段), resume 时复原, 不会串到另一条车道。
#
# 其他工作(默认车道)——glm5.2(百炼 key):
# JARVIS_SETTINGS=/Users/you/.claude/glm5.2.json
# Terraform 线(tf_customer/tf_provider + alicloud_/terraform-provider 标题)——ideamo→ideamore→glm5.2 兜底:
# 未设时自动回退 JARVIS_SETTINGS(分层是 opt-in; 只配 JARVIS_SETTINGS 时两条车道等价于旧版单链)。
# JARVIS_SETTINGS_TF=/Users/you/.claude/ideamo.json,/Users/you/.claude/ideamore.json,/Users/you/.claude/glm5.2.json
#
# Jarvis=cc: 重型升级用启动脚本。默认 ~/claude-start.sh。设了则全覆盖、不分车道。
# JARVIS_CC=/Users/you/claude-start.sh
# Tata 默认一轮一进程, 不保留常驻 claude; 确需保温时显式设为 1。
JARVIS_TATA_RESIDENT=0
# resident 模式下 Tata 常驻进程空闲多少分钟回收, 默认 30。
JARVIS_TATA_IDLE_MIN=30
# resident 模式下 Tata 常驻进程并发上限, 超额 LRU 踢最旧, 默认 10。
JARVIS_TATA_MAX=10
# resident 模式下启动即预热 N 个未绑定 generic 常驻进程; 默认 3。
JARVIS_TATA_PREWARM=3
# 群聊历史上下文（默认关闭）：只在群内 @ Tata 触发时，使用 Stream 回调中的当前群 ID。
# 每次读取前都校验 DWS 登录人（默认辰羿）和 Tata 仍在回调对应群；不接受用户/模型指定其他群。
# DWS 登录人不在群时不启动 Tata、不搜索消息，直接提示群主/管理员添加辰羿后重新 @ Tata。
# DWS 失败时 fail-closed 为仅处理当前 @ 消息，不影响 Tata 回复。
# JARVIS_TATA_DWS_HISTORY=1
# JARVIS_TATA_DWS_PROFILE=
# JARVIS_TATA_DWS_USER_ID=320687
# JARVIS_TATA_DWS_LOOKBACK_MIN=30
# JARVIS_TATA_DWS_MAX=20
# JARVIS_TATA_DWS_TIMEOUT=15
# JARVIS_DWS_BIN=dws
# override path to dingtalk-ai-card streaming.py if not at default skill location
# DINGTALK_SKILL=/Users/you/.claude/skills/dingtalk-ai-card/scripts/streaming.py
# Scan scheduler: push new work items to DingTalk group (default: 辰羿,过载 group)
# JARVIS_NOTIFY_GROUP=cidy1mv+qvMEybkqTXcsXTOeQ==
# 周期频率由 bridge/scheduler/jobs.yaml 中的显式 Job 配置唯一管理；不要再设置 JARVIS_SCAN_INTERVAL。
# Whose Aone queue to scan. Unset = the jarvis account itself (a1 whoami). Set to another
# staffId (e.g. 320687 辰羿) to watch that person's assigned items — writes still go out as
# jarvis (scan/claim/wrap route through bin/a1id), so this only changes the target.
# JARVIS_SCAN_ASSIGNEE=320687
# Board auto-sync: after each scan, board.sh JSON is pushed to AutomationAgent /board dashboard.
# Reuses JARVIS_HTML_REPORT_TOKEN (bootstrap/.env) for bearer auth and
# JARVIS_HTML_REPORT_BASE_URL (default https://pre-agent.aliyun-inc.com) for the target.
# No extra config needed — if html-report-preview works, board sync works.

# --- Task / EphemeralJob 执行模型 ---
# 领域路由只有两种结果：需恢复的 Task 与可丢弃的 EphemeralJob。
# 默认 Task 类型：ticket,revisit,persona,wake,pr_ci_fix,pr_comment_reply。
# probe/card/preflight 等未列入的入口是 EphemeralJob，不创建 Task/Session。
# AutomationAgent 基地址（client 自动追加 /api/jarvis/v1）。Task 路径必填；
# 未配置或控制面失败时 Task fail-closed，不会回退到本地执行。
# JARVIS_CONTROL_PLANE_BASE_URL=https://pre-agent.aliyun-inc.com
# 可选 Bearer token；真实 token 只放 bridge/jarvis.env，不入库。
# JARVIS_CONTROL_PLANE_TOKEN=
# JARVIS_CONTROL_PLANE_TIMEOUT=10
# JARVIS_CONTROL_PLANE_RETRY_SEC=5
# scheduler 角色从控制面分页拉取 UNKNOWN/due RETRY_WAIT Aone operation，持 recovery
# token 只读核验 comment/status/tag 后收敛回执；worker 角色不启动。读不确定即 release，
# 不碰 Aone。token 由 worker key + operation id 稳定派生，可跨 tick 续接同一租约。
# JARVIS_EXTERNAL_RECOVERY_ENABLE=1
# 周期由 jobs.yaml 的 external.recovery 管理；不要再设置 JARVIS_EXTERNAL_RECOVERY_INTERVAL。
# JARVIS_EXTERNAL_RECOVERY_AONE_TIMEOUT=30
# 默认租约由 10 分钟控制面故障容忍窗口 + 60 秒 fencing 尾窗组成。
# 连续 5xx/网络故障未满 10 分钟时继续运行；进入最后 60 秒后 fail-closed，
# 确保服务端回收前旧 Worker 已停止。
# JARVIS_LEASE_SECONDS=660
# JARVIS_LEASE_SAFETY_MARGIN_SEC=60
# JARVIS_LEASE_POLL_SEC=2
# JARVIS_WORKER_HEARTBEAT_SEC=30
# JARVIS_SESSION_HEARTBEAT_SEC=30
# Claimed-ticket health cadence 由 jobs.yaml 的 aone.claim-health 管理；状态策略仍在
# config/pools.json claim.health。不要再设置 JARVIS_CLAIM_HEALTH_INTERVAL_SEC。
# 交互式 Codex/Claude 使用相同的 660s task lease，独立 sidecar 每 30s 续租。
# Codex Desktop Stop 后保留 10 分钟同 Task 续问窗口。
# JARVIS_INTERACTIVE_LEASE_SECONDS=660
# JARVIS_INTERACTIVE_HEARTBEAT_SEC=30
# JARVIS_INTERACTIVE_TURN_GRACE_SEC=600
# 工具只读 sidecar 写入的本地 Lease Proof；剩余租约低于 60s 时 fail-closed。
# JARVIS_INTERACTIVE_LEASE_SAFETY_MARGIN_SEC=60
# Codex turn 自动挂起后保持同 Worker affinity 2 小时，再由 waitExpireAt 进入恢复调度。
# JARVIS_INTERACTIVE_AFFINITY_SEC=7200
# Stop 事件丢失时的硬兜底：活跃 turn 连续 12 小时无 prompt/tool hook 活动即挂起；设为 0 可关闭。
# JARVIS_INTERACTIVE_ACTIVE_TURN_TTL_SEC=43200
# 无当前任务的 Codex 会话空闲多久后将 Worker 标为 OFFLINE 并退出 sidecar；下次 prompt 自动重启。
# 设为 0 可关闭，默认 8 小时。
# JARVIS_INTERACTIVE_IDLE_TTL_SEC=28800
# 正常退出先等在途 Task Session，超时则 retryable fail+停进程，由下一租约恢复。
# JARVIS_WORKER_DRAIN_TIMEOUT=30
# fence/心跳失效后先 SIGTERM，超过宽限仍存活则 SIGKILL 并回收。
# JARVIS_TASK_STOP_GRACE_SEC=5
# Task Claude 由独立进程组 guardian 托管：PID 完成 fenced 持久绑定前不放行；
# bridge 崩溃/SIGKILL 或 CLI 正常退出时，guardian 会清理整组及残留后台孙进程。
# JARVIS_TASK_GUARD_GRACE_SEC=2

# --- 多机部署 · 角色分工 (docs/multi-worker-deployment.md) ---
# JARVIS_BRIDGE_ROLE=scheduler|worker (default scheduler).
#   scheduler: run bridge/run.sh; it starts both PersistenceExecutor and the sole
#              SchedulerEngine for scan/claim-health/nudge/reply/PR/recovery.
#              Local state: pr-watch.json / event-ledger / claimed observations — not
#              sharable across hosts. **Exactly one host in the fleet must run scheduler.**
#              run.sh rejects worker or unknown role before Scheduler registration.
#   worker:    executor-only — PersistenceExecutor.start() then returns. Leases Tasks
#              from control plane by its own workerKey; spawns headless claude. No
#              scheduling, no local sensor state. Every added worker grows total
#              lease capacity by JARVIS_DISPATCH_MAX slots. Zero coordination with
#              other workers needed — fenced claimTask on the control plane is the
#              only cross-worker interlock.
# Deployment shape today: 1× scheduler (macmini) + N× worker (Linux). Fleet total
# concurrency = sum(JARVIS_DISPATCH_MAX) across all hosts.
# pidfile/log split by role: bot.{pid,log} (scheduler) vs bot-worker.{pid,log} (worker).
# JARVIS_BRIDGE_ROLE=scheduler

# --- SchedulerEngine 注册表与渐进迁移 ---
# 新 Engine 固定注册 workerKey=bridge-scheduler，复用普通 Task 控制面 token；hostId 只记录
# 实际运行位置，不参与授权。控制面通过固定 workerKey、当前 processUuid、ACTIVE 状态和 heartbeat
# 保证只有一个 Scheduler 进程；固定 Scheduler 显式注册 capabilities.dispatch.pull=false，不参与普通
# Task queue pull。
# JARVIS_SCHEDULER_HEARTBEAT_SEC=30
# JARVIS_SCHEDULER_POLL_SEC=5
# bridge/run.sh 与 install-launchd.sh 等待新实例完成 Scheduler 注册、恢复并进入 READY。
# JARVIS_SCHEDULER_READY_WAIT=30
# JARVIS_SCHEDULER_DRAIN_TIMEOUT_SECONDS=30
#
# 新链路注册内容见 bridge/scheduler/jobs.yaml；它是全部周期 Job 的唯一显式注册表。
# runner 通过 handler_key 解析；daily.probe 只从 Scheduler 执行，runner 只执行一次业务 tick。
# 每个 Job 的 enabled 布尔字段由 jobs.yaml 决定。修改后重启 Scheduler 同步控制面；
# enabled=false 时 Job 保持注册但不执行。
#
# 不同 Job 可按以下上限并发；同一 job_key 永不 overlap。默认 4。
# JARVIS_SCHEDULER_MAX_CONCURRENCY=4
# Probe 摘要原子落盘的额外重试次数；耗尽后永久失败，避免重放已完成的外部副作用。
# JARVIS_PROBE_SUMMARY_WRITE_RETRIES=2
#
# Aone 任务归属快照每 300 秒由 jobs.yaml 的 aone.workitem-ownership 同步到 Board。
# 缓存按 Aone modified 复用未变化项，避免重复读取评论；目录需对 Scheduler 进程可写。
# JARVIS_AONE_OWNERSHIP_CACHE=/path/to/jarvis/.my-day/bridge/aone-workitem-ownership-cache.json
# 首次或变化项的评论读取并发上限（1-32，默认 8）。
# JARVIS_AONE_OWNERSHIP_COMMENT_WORKERS=8
# Aone 批量列表大小（1-500，默认 100）及单次 a1 读取超时秒数（默认 120）。
# JARVIS_AONE_OWNERSHIP_BATCH_SIZE=100
# JARVIS_AONE_OWNERSHIP_A1_TIMEOUT=120
#
# 计划内 restart 会先关闭新 job admission、登记 DRAINING，再等待在途 job 完成。
# Scheduler、Bot、Persistent Worker 共享最多 30 秒停机预算；超时后只强制清理当前
# supervisor 精确管理的 PID，再启动替代实例。未完成的 Session handoff 由 lease
# expiry/recovery 按原 runtimeSessionId/fence 收敛，不绕过控制面所有权。

# --- Task 发现与授权 ---
# 1=scan 发现可派发工单后直接创建控制面 Task（默认）；
# 0=持久写入待授权列表，钉钉回复「处理 #id / 全部处理」后才创建 Task。
# JARVIS_AUTO_DISPATCH=1
# 空闲机会式消化积压: 1=开(默认) 0=纯新单。仅 auto 生效——无新单/更新单的 tick，
# 按 优先级→建单时间最多 upsert JARVIS_BACKLOG_MAX_SLOTS 个积压 Task。
# Task 的 taskKey + desiredRevision 由控制面幂等收敛，不受本地 EphemeralExecutor 容量影响。
# JARVIS_BACKLOG_DRAIN=1
# PersistenceExecutor 与 EphemeralExecutor 共享的机器执行槽上限，默认 3。
# JARVIS_DISPATCH_MAX=3
# 积压 Task 每轮最多 upsert 几个，默认 1。
# JARVIS_BACKLOG_MAX_SLOTS=1

# --- EphemeralExecutor（旧 Bridge 的其他一次性作业）---
# 超过 max+queue 的本地临时作业排队深度就丢弃并播报，默认 20。
# JARVIS_DISPATCH_QUEUE_MAX=20
# 本地临时作业软去重窗口(秒)，默认 86400=24h；不用于 Task。
# JARVIS_DISPATCH_DEDUP_TTL=86400
# 自动派发/probe/revisit 播报落点, 缺省=JARVIS_NOTIFY_GROUP。
# JARVIS_BROADCAST_TARGET=
# 播报会话类型, 默认 group。
# JARVIS_BROADCAST_TYPE=group
# headless 单轮超时(秒), 默认 43200=12h。
# JARVIS_DISPATCH_TIMEOUT=43200
# 每日 tf-probe 探测轮的启停和触发时间均由 bridge/scheduler/jobs.yaml 定义。
# 当前 revision 2 为 enabled:false，只验证控制面注册/看板；真实启用需单独授权，
# 将 revision 升至 3 并设置 enabled:true。
# 每日 jarvis-idle 重访轮: 1=开(默认) 0=关; hour 触发。Terraform 全量开放 idle 单做
# >=8天无实质进展判定并由 RD 双通道幂等催办；非 Terraform 维持人工门 headless 重访。
# JARVIS_REVISIT_SCHED=1
# JARVIS_REVISIT_HOUR=9
# JARVIS_REVISIT_MAX=100
# JARVIS_REVISIT_STALE_DAYS=8
# 数字人评论区自主协作跨会话补位 PersonaScheduler (loops/persona-collab.md):
# 只扫带 jarvis-idle 标签的池内工单(claimed=同会话接力正在进行,不需补位);逐条新评论按
# [[PERSONA-HANDOFF:{...}]] 哨兵或**显式** @ 数字人(裸角色名不算)触发对应角色的 headless jarvis
# 编排层继续接力。服务端按 dispatch_count 硬护栏,不信任评论自报 round。
# **默认关闭(灰度期)**:显式设 =1 才启用;验证稳定后可视情况改默认。
# JARVIS_PERSONA_WATCH=1
# 轮询间隔秒, 默认 600(时效门 cutoff = max(24h, 2*interval),防历史评论回放风暴)。
# JARVIS_PERSONA_INTERVAL=600
# 单工单接力次数上限, 默认 6; 达到即改派升级 @过载(484483) 收尾, 不再自动接力。
# 人类显式 @ 数字人触发会重置计数(人工重新授权预算)。
# JARVIS_PERSONA_MAX_ROUNDS=6
# 数字人昵称映射(可选): 三数字人登录后若显示名不含 terraform 字样, 用此 env 兜底告诉 bridge
# 哪个显示名对应哪个 role。格式: "terraform-pd=昵称A,terraform-rd=昵称B,..."
# JARVIS_PERSONA_NICKS=

# ── 自动派发范围限定（灰度安全阀；★默认已收窄，非全量★）─────────
# 机制未大规模实测，**默认就限定灰度**：仅自动处理 tf_provider 池 + 2024-01-01 前创建的单
# (assignee 另由 JARVIS_SCAN_ASSIGNEE 限定)。即使漏配 env 也不会全量放开(fail-safe)。
# 不在范围的单静默 skip(不派、不播报)；created 缺失的单保守视为不在范围(不派)。
# 下面两行写的就是默认值；要改就改值，要放宽就设为空：
# JARVIS_DISPATCH_POOLS=tf_provider          # 逗号分隔池 key 白名单; 默认 tf_provider; 设空=所有池
# JARVIS_DISPATCH_CREATED_BEFORE=2024-01-01  # 只派此日期前创建的单; 默认 2024-01-01; 设空=不限
# 验证稳定后放宽为全量：两者都显式设空(JARVIS_DISPATCH_POOLS= 且 JARVIS_DISPATCH_CREATED_BEFORE=)。
#
# 运行时暂停(无需重启): touch .my-day/bridge/pause 停止新派发, rm 恢复; 在途 worker 不受影响。
# 中断在途单: 目前需停/重启 bridge；服务端 reaper 收敛 Session，Aone runner 告警 stale claim。

# ── claim 跨机安全（可选；默认 settle=0 = 单机现状，claim 零评论污染）──────
# 部署 A（推荐 / 当前形态）：只有一台主机跑 bridge 自动派发，其余主机人工交互
#   claude/Codex。→ **保持 JARVIS_CLAIM_SETTLE=0（默认，不要开仲裁）**。此时 claim
#   只打 jarvis-claimed 标签、不写任何仲裁评论；跨机互斥靠该标签(Aone 可见)+ scan 的
#   标签 skip，极小概率的双 claim 由控制面 fence 与 stale-claim 告警暴露。开仲裁反而会每次 claim
#   往工单刷一条 jarvis-claim 评论、污染业务上下文，此场景得不偿失。
# 部署 B：多台主机都跑 bridge 自动派发（会对称抢同一单）。→ 每台都设
#   JARVIS_CLAIM_SETTLE=3 启用仲裁；漏设的机器 claim 不写仲裁评论 → 仲裁不对称 →
#   可能双 claim。代价是每次 claim 多一条 jarvis-claim 评论。(注：多 bridge 还需 lease
#   选主 + leader 门控，本仓暂未实现。)
# JARVIS_SELF_HOST=            # 本机标识(仲裁用), 默认 scutil --get ComputerName || hostname
# JARVIS_CLAIM_SETTLE=0        # >0 启用 claim 跨机仲裁沉降秒数; 默认 0=点读(单 bridge 推荐)
