name: Terrafrom Basic Test Process
on:
  push:
    paths:
      - .github/workflows/acctest-terraform-basic.yml
      - alicloud/*.go
    branches:
      - master
  pull_request:
    paths:
      - .github/workflows/acctest-terraform-basic.yml
      - alicloud/*.go

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Go Version
        uses: actions/setup-go@v2
        with:
          go-version: '1.19.3'

      - name: Get dependencies
        run: |
          go install golang.org/x/tools/cmd/goimports@latest
          go mod tidy

      - name: fmtcheck
        run: |
          make fmtcheck

      - name: importscheck
        run: |
          make importscheck

      - name: vet
        run: |
          make vet

#      - name: test
#        run: |
#          make test

  Integration-Test:
    runs-on: ubuntu-latest
    environment: IntegrationTest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: '3'
      - uses: actions/setup-go@v2
        with:
          go-version: '1.19.3'
      - name: Use variables
        run: |
          echo "TF variable : ${{ vars.TF_ACC }}"
          echo "region variable : ${{ ALICLOUD_REGION }}"
      - name: grant permissions
        run: |
          chmod +rwx scripts/attribute_compatibility_check.sh
          chmod +rwx scripts/attribute_consistency_check.sh
          chmod +rwx scripts/integration_test_check.sh
#          export TF_ACC=1
#      - name: Attribute Compatibility Check
#        run: |
#          sudo bash scripts/attribute_compatibility_check.sh
#      - name: Attribute Consistency Check
#        run: |
#          sudo bash scripts/attribute_consistency_check.sh
      - name: Integration Test Check
        run: |
          sudo bash scripts/integration_test_check.sh ${{ ALICLOUD_ACCESS_KEY }} ${{ ALICLOUD_SECRET_KEY }} ${{ ALICLOUD_REGION }}