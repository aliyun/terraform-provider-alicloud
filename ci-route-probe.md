# CI Route Probe

This temporary document verifies that a documentation-only change is classified
as irrelevant by the pull request policy. It does not change provider behavior
or runtime configuration.
