# bootstrap/skills-mirror-allowlist
# skills-mirror-check.sh 用:列出必须在 .claude/skills 与 .agents/skills 之间保持一致的文件路径。
#
# 路径相对于 .claude/skills 或 .agents/skills(两侧同名同层)。
# 只列**无 agent-specific 差异**的纯内容文件——如决策规则、共享脚本。
# 不要列 SKILL.md、入口文案或带 AGENTS.md/CLAUDE.md 引用、~/.Codex/vs ~/.claude/ 路径、
# Codex vs Claude Code 水印的文件——那些 codex 与 claude 侧必然合法分歧。
#
# 每行一个相对路径;# 起头视为注释;空行忽略。
# 变更历史:
#   2026-07-02 初版,先纳管 aone-triage 决策规则 + 只读同步脚本。

aone-triage/references/tf-customer-request-routing.md
aone-triage/scripts/sync-provider.sh
